# SurfBook Service — 환경 변수 예시
# 이 파일을 .env 로 복사하여 실제 값을 입력하세요
# H2 인메모리 DB 사용 시 아래 항목은 불필요합니다 (개발용)

# PostgreSQL (운영 환경)
# DB_URL=jdbc:postgresql://localhost:5432/surfshopdb
# DB_USERNAME=surfshop_user
# DB_PASSWORD=your_password
